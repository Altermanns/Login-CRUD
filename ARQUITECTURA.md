# 🏗️ ARQUITECTURA DEL PROYECTO - LoginCRUD/Texcore

## 📐 Diagrama de Arquitectura en Capas

```
┌─────────────────────────────────────────────────────────────────┐
│                         USUARIOS                                 │
│                  (Roles: Admin, Operario, Preparador)           │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                     CAPA DE URLS                                 │
│  urls.py → Enrutamiento a vistas específicas por dominio        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                   CAPA DE VISTAS (Presentación)                  │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │ auth_views   │  │ materia_views│  │ user_views   │         │
│  │  - inicio    │  │  - listar    │  │  - listar    │         │
│  │  - login     │  │  - crear     │  │  - crear     │         │
│  │  - logout    │  │  - editar    │  │  - editar    │         │
│  └──────────────┘  │  - eliminar  │  │  - eliminar  │         │
│                    └──────────────┘  └──────────────┘         │
│  ┌──────────────┐  ┌─────────────────────────────┐            │
│  │ dashboard_   │  │ preparacion_views           │            │
│  │   views      │  │  - listar                   │            │
│  │  - admin     │  │  - crear                    │            │
│  │  - operario  │  │  - detalle                  │            │
│  │  - preparador│  │  - iniciar/completar        │            │
│  └──────────────┘  │  - agregar_detalle          │            │
│                    └─────────────────────────────┘            │
│                                                                  │
│  Responsabilidad: Request/Response, Validación de Forms,        │
│                   Renderizado de Templates                      │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│               CAPA DE SERVICIOS (Lógica de Negocio)              │
│                                                                  │
│  ┌──────────────────┐  ┌──────────────────┐                    │
│  │ auth_service     │  │ materia_service  │                    │
│  │  - authenticate  │  │  - get_all       │                    │
│  └──────────────────┘  │  - crear         │                    │
│                        │  - actualizar    │                    │
│  ┌──────────────────┐  │  - eliminar      │                    │
│  │ dashboard_service│  │  - validar_stock │                    │
│  │  - admin_stats   │  └──────────────────┘                    │
│  │  - operario_stats│                                           │
│  │  - preparador_st │  ┌─────────────────────────────┐         │
│  │  - reporte_stats │  │ preparacion_service         │         │
│  └──────────────────┘  │  - crear_preparacion        │         │
│                        │  - iniciar_proceso    ⚛️    │         │
│                        │  - completar_proceso  ⚛️    │         │
│                        │  - validar_stock            │         │
│                        │  - agregar_detalle          │         │
│                        │  - filtrar                  │         │
│                        └─────────────────────────────┘         │
│                                                                  │
│  Responsabilidad: Lógica de Negocio, Validaciones,             │
│                   Transacciones Atómicas, Cálculos             │
│                                                                  │
│  ⚛️ = @transaction.atomic (integridad garantizada)              │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                   CAPA DE FORMULARIOS                            │
│                                                                  │
│  ┌──────────────┐  ┌──────────────────┐  ┌──────────────┐     │
│  │ MateriaForm  │  │ PreparacionForm  │  │ Detalle      │     │
│  │              │  │                  │  │ PreparacionF │     │
│  └──────────────┘  └──────────────────┘  └──────────────┘     │
│                                                                  │
│  Responsabilidad: Validación de Datos de Entrada                │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                   CAPA DE MODELOS (ORM)                          │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │ User         │  │ Materia      │  │ Preparacion  │         │
│  │ (Django)     │  │  - tipo      │  │  Materia     │         │
│  └──────┬───────┘  │  - cantidad  │  │  - estado    │         │
│         │          │  - lote      │  │  - tipo_proc │         │
│  ┌──────▼───────┐  │  - fecha     │  │  - cantidad  │         │
│  │ Profile      │  └──────────────┘  └──────────────┘         │
│  │  - role      │                                               │
│  │  - created   │  ┌──────────────┐                            │
│  └──────────────┘  │ Detalle      │                            │
│                    │ Preparacion  │                            │
│                    │  - temp      │                            │
│                    │  - humedad   │                            │
│                    │  - rendim.   │                            │
│                    └──────────────┘                            │
│                                                                  │
│  Responsabilidad: Definición de Datos, Relaciones, Propiedades  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                   CAPA DE SEGURIDAD                              │
│                                                                  │
│  ┌──────────────────────────────────────────────────────┐       │
│  │ decorators.py                                        │       │
│  │  - @admin_required                                   │       │
│  │  - @operario_required                                │       │
│  │  - @preparador_required                              │       │
│  │  - @admin_or_operario_required                       │       │
│  │  - @admin_or_preparador_required                     │       │
│  └──────────────────────────────────────────────────────┘       │
│                                                                  │
│  Responsabilidad: Control de Acceso por Roles                   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                   BASE DE DATOS (SQLite)                         │
│                                                                  │
│  Tables: auth_user, Texcore_profile, Texcore_materia,          │
│          Texcore_preparacionmateria, Texcore_detalleprep       │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Flujo de una Petición (Ejemplo: Crear Preparación)

```
1. Usuario (Preparador) → POST /preparaciones/crear/
                          ↓
2. urls.py → preparacion_views.crear_preparacion
                          ↓
3. Vista valida Form → PreparacionMateriaForm.is_valid()
                          ↓
4. Vista llama Servicio → preparacion_service.crear_preparacion()
                          ↓
5. Servicio valida stock → validar_stock_disponible()
                          ↓
6. @transaction.atomic → BEGIN TRANSACTION
                          ↓
7. Servicio crea registro → PreparacionMateria.objects.create()
                          ↓
8. COMMIT TRANSACTION
                          ↓
9. Vista muestra mensaje → messages.success()
                          ↓
10. Redirect → detalle_preparacion
```

## 🎯 Separación de Responsabilidades

| Capa | Responsabilidad | Ejemplo |
|------|----------------|---------|
| **URLs** | Routing | `path('preparaciones/crear/', ...)` |
| **Vistas** | Request/Response | `return render(request, template, context)` |
| **Servicios** | Lógica de Negocio | `@transaction.atomic def crear_preparacion()` |
| **Formularios** | Validación de Input | `form.is_valid()` |
| **Modelos** | Estructura de Datos | `class Materia(models.Model)` |
| **Decoradores** | Seguridad | `@preparador_required` |

## 🔐 Control de Acceso por Rol

```
┌─────────────────────────────────────────────────┐
│                    ADMIN                        │
│  ✅ Dashboard Admin                             │
│  ✅ Gestión de Usuarios (CRUD)                  │
│  ✅ Ver Materias                                │
│  ✅ Ver Preparaciones                           │
│  ✅ Eliminar Preparaciones                      │
│  ✅ Reportes Completos                          │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│                  OPERARIO                       │
│  ✅ Dashboard Operario                          │
│  ✅ Materias (CRUD completo)                    │
│  ✅ Ver Materias                                │
│  ❌ Gestión de Usuarios                         │
│  ❌ Preparaciones                               │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│                PREPARADOR                       │
│  ✅ Dashboard Preparador                        │
│  ✅ Preparaciones (CRUD)                        │
│  ✅ Iniciar/Completar Preparaciones             │
│  ✅ Agregar Detalles Técnicos                   │
│  ✅ Reportes de Preparaciones                   │
│  ❌ Gestión de Usuarios                         │
│  ❌ CRUD de Materias                            │
└─────────────────────────────────────────────────┘
```

## 📊 Optimizaciones Implementadas

### 1. Queries Optimizadas
```python
# N+1 Problem SOLVED
materias = Materia.objects.select_related('usuario_registro')
preparaciones = PreparacionMateria.objects.select_related(
    'materia_prima', 'usuario_preparador'
).prefetch_related('detalles')
```

### 2. Transacciones Atómicas
```python
@transaction.atomic
def completar_preparacion_proceso(preparacion, usuario):
    # Update stock
    materia.cantidad -= cantidad_procesada
    materia.save()
    
    # Update preparacion
    preparacion.estado = 'completada'
    preparacion.save()
    
    # BOTH or NONE - No partial updates
```

### 3. Validaciones Centralizadas
```python
# En servicio - reutilizable en múltiples vistas
is_valid, msg = validar_stock_disponible(materia, cantidad)
if not is_valid:
    raise ValueError(msg)
```

## 🎓 Beneficios de la Arquitectura

1. **✅ Mantenibilidad**: Fácil encontrar y modificar código
2. **✅ Testabilidad**: Cada capa es testeable independientemente
3. **✅ Escalabilidad**: Fácil agregar nuevas funcionalidades
4. **✅ Reutilización**: Servicios compartidos entre vistas
5. **✅ Integridad**: Transacciones atómicas garantizan consistencia
6. **✅ Seguridad**: Control de acceso centralizado
7. **✅ Performance**: Queries optimizadas reducen carga en BD

---

**Fecha**: Noviembre 20, 2025  
**Versión**: 2.0 (Refactorizada)  
**Estado**: ✅ Producción Ready
